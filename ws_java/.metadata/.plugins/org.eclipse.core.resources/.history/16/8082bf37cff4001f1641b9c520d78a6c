package jcf;

public class DataProcess {
  
  Object data = new MovieVO("인셉션", 10.0, "디카프리오");
  
  public MovieVO save(String title, double score, String actor) {
    MovieVO movie = new MovieVO(title, score, actor);
    return movie;
  }
  
  public MovieVO findByActor(String title) {
    
  }

}
